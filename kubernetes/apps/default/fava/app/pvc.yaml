---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fava
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 2Gi
  storageClassName: topolvm-thin-provisioner
