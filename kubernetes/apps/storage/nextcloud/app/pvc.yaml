---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-nfs
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: nfs-ugreen-hdd
  resources:
    requests:
      storage: 100Gi
