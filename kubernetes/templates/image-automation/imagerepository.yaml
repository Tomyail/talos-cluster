---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: APPNAME
  namespace: NAMESPACE
spec:
  image: REGISTRY_URL
  interval: 1m
  secretRef:
    name: APPNAME-registry-secret
